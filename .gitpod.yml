image:
  file: .gitpod.Dockerfile

tasks:
  - before: |
      export GRADLE_USER_HOME="/workspace/.gradle"
      export M2_HOME="/workspace/.m2"
  - init: |
      ./gradlew build

jetbrains:
  intellij:
    prebuilds:
      version: stable
